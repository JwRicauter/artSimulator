import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles((theme) => ({

    button: {
        margin: theme.spacing(1),
    },

    languageIcon: {
        height: 50,
        width: 50,
    },
    
}));

export { styles };
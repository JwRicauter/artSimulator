import { makeStyles } from "@material-ui/core/styles";


const styles = makeStyles((theme) => ({
  logo: {
    height: "10vh",
    width: "inherit",
    maxHeight: 80,
    padding: "1px",
  },
}));

export { styles };
